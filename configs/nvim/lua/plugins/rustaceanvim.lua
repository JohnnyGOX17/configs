return {
  -- NOTE: not using Mason's rust-analyzer, install via `$ rustup component add rust-analyzer`
  'mrcjkb/rustaceanvim',
  version = '^6', -- Recommended
  lazy = false, -- This plugin is already lazy
}
